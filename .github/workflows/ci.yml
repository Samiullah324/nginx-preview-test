name: CI
on:
  pull_request:
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CI checks
        run: |
          echo "Running CI checks..."
          echo "CI checks passed"
